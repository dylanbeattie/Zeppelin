// Zeppelin.peg : parsing expression grammar for Zeppelin

output<OutputNode> = 'say' _ n:number { new OutputNode(n) }

number<NumberNode> = n:("" [0-9]+) { new NumberNode(decimal.Parse(n)) }

whitespace = [\t ]
_ = whitespace+

EOL = '\n'
EOF = !.
